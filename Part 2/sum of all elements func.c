#include<stdio.h>
void sum(int[],int);
int main()
{
    int a[100],i,n,s=0;
    printf("Enter the No. of elements\n");
    scanf("%d",&n);
    printf("enter the numbers\n");
    for(i=0;i<n;i++)
        scanf("%d",&a[i]);
        sum(n,s);
}
f(float[],float,int);
void sum(int x[],int s)
{
    int i,n;
    s=s+x[i];
    for(i=0;i<n;i++)
        printf("%d",x[i]);
        printf("%d",s);
}
